# build output
build/
*.o
*.out

# coverage artifacts (docs/coverage/ is generated but tracked)
*.gcda
*.gcno
*.gcov
*.info

# the generated docs pages (traceability, verification, coverage) are
# committed as snapshots and regenerated by CI; nothing under docs/ is ignored

# editor / OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
